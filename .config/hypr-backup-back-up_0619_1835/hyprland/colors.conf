# exec = export SLURP_ARGS='-d -c FFDCC6BB -b 66402544 -s 00000000'

general {
    col.active_border = rgba(F5DDDB39)
    col.inactive_border = rgba(A78A8730)
}

misc {
    background_color = rgba(1C100FFF)
}

plugin {
    hyprbars {
        # Honestly idk if it works like css, but well, why not
        bar_text_font = Rubik, Geist, AR One Sans, Reddit Sans, Inter, Roboto, Ubuntu, Noto Sans, sans-serif
        bar_height = 30
        bar_padding = 10
        bar_button_padding = 5
        bar_precedence_over_border = true
        bar_part_of_window = true

        bar_color = rgba(1C100FFF)
        col.text = rgba(F5DDDBFF)


        # example buttons (R -> L)
        # hyprbars-button = color, size, on-click
        hyprbars-button = rgb(F5DDDB), 13, 󰖭, hyprctl dispatch killactive
        hyprbars-button = rgb(F5DDDB), 13, 󰖯, hyprctl dispatch fullscreen 1
        hyprbars-button = rgb(F5DDDB), 13, 󰖰, hyprctl dispatch movetoworkspacesilent special
    }
}

windowrulev2 = bordercolor rgba(97CBFFAA) rgba(97CBFF77),pinned:1